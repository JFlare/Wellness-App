<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wellnessapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pstoryMenu"/>
        <activity android:name=".ContentTable"/>
        <activity android:name=".SignIn"/>
        <activity android:name=".Anatomy" />
        <activity android:name=".ReadSpeak" />
        <activity android:name=".CrosswordMenu" />
        <activity android:name=".MariaCrossword"/>
        <activity android:name=".SickCrossword"/>
        <activity android:name=".FlashCards" />
        <activity android:name=".MariaStory"/>
        <activity android:name=".FatimaStory"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>